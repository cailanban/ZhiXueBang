# 智学帮系统 — 测试说明书

> 版本：v13  
> 赛题：A3 — 基于大模型的个性化资源生成与学习多智能体系统开发  
> 测试日期：2026-07-12  
> 测试人员：开发团队  
> **状态：⚠️ 本文档为测试计划，实际自动化测试正在建设中。已通过的手动验证标注为「手动通过」，自动化测试标注为「待自动化」。**

---

## 1. 测试环境

| 项目 | 说明 |
|------|------|
| 浏览器 | Chrome 125+ / Edge 124+（桌面端主要测试） |
| Node.js | 18.x（本地 lint / build 验证） |
| 运行时 | Deno 1.x（Supabase Edge Functions） |
| 数据库 | Supabase PostgreSQL 15（托管云端） |
| 前端框架 | React 18 + TypeScript + Vite + Tailwind CSS + shadcn/ui |
| 部署方式 | Supabase Edge Functions（serverless） |
| AI 接口 | DeepSeek Chat V4（deepseek-chat） |
| 访问地址 | https://616653b8b4a6430e99d318747e490cae.app.codebuddy.work/ |

---

## 2. 测试范围

| 模块 | 页面/功能 | 测试类型 |
|------|-----------|---------|
| 用户体系 | 登录、注册、Profile 设置 | 手动通过 |
| 学习画像 | 对话式画像构建（ProfileAgent） | 手动通过 |
| 辅导系统 | TutorPage SSE 流式对话、Mermaid 渲染 | 手动通过 |
| 多智能体资源生成 | ResourcesPage（7 Agent DAG 编排 + 容错） | 手动通过 |
| 学习路径 | PathPage（deepseek-path + recommendedResources） | 手动通过 |
| 课程管理 | CourseDetailPage（章节、练习、生成配图） | 手动通过 |
| 评估系统 | EvaluationPage（薄弱点标签云 + AI 综合评估） | 手动通过 |
| 知识库 | 上传、检索（个人知识库） | 手动通过 |
| 可视化 | VisualizationPage（Mermaid/可视化生成） | 手动通过 |
| PPT 生成 | generate-ppt Edge Function | 手动通过 |
| 系统状态 | StatusPage（真实健康检查 API） | 手动通过 |
| 安全 | RLS 权限验证、JWT 鉴权 | 手动通过 |
| 自动化测试 | 单元测试 / 集成测试 / E2E | ⚠️ 待建设 |

---

## 3. 测试用例

### TC-01 用户注册与登录

| 步骤 | 预期结果 |
|------|---------|
| 访问首页，点击「注册」，填写邮箱+密码 | 注册成功，跳转到主页 |
| 使用已注册邮箱密码登录 | 登录成功，显示用户头像和姓名 |
| 使用错误密码登录 | 提示「邮箱或密码错误」 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-02 对话式学习画像构建

| 步骤 | 预期结果 |
|------|---------|
| 进入「学习画像」页，点击「AI实时更新」 | AI 开始分析学习数据 |
| 等待分析完成 | 画像卡片生成，显示薄弱点、学习风格等 |
| 刷新页面 | 画像数据持久化，不丢失 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-03 SSE 流式辅导对话

| 步骤 | 预期结果 |
|------|---------|
| 进入「AI 辅导」，输入「解释 Java 多态」 | AI 回复逐字流式输出（实时追加） |
| 等待完整回复 | 回复末尾包含 mermaid 流程图代码块，并渲染为图表 |
| 连续发送多轮消息 | 历史上下文保留，AI 记忆前文 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-04 多智能体 DAG 资源生成（正常流程）

| 步骤 | 预期结果 |
|------|---------|
| 进入「资源生成」，选「讲义」，输入「Java 线程池」 | 7 个 Agent 并行执行，显示 Agent 执行链 |
| 等待完成 | 生成高质量讲义内容，防幻觉审核通过（score >= 70） |
| 点击「下载 MD」 | 下载 Markdown 文件 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-05 多智能体 DAG 容错（单 Agent 失败）

| 步骤 | 预期结果 |
|------|---------|
| 模拟 MindmapAgent 超时（网络抖动） | 其余 4 个 Agent 正常产出，整体不崩溃 |
| 查看 Agent 执行链 | MindmapAgent 显示「error」，其他显示「success」 |
| 检查返回内容 | 讲义/练习题/代码案例正常返回 |

**结果**：✅ 手动通过（Promise.allSettled 容错）

---

### TC-06 学习路径规划 + 推荐资源

| 步骤 | 预期结果 |
|------|---------|
| 进入「学习路径」，输入「Spring Boot」 | 生成 4 阶段学习路径 |
| 展开某阶段 | 显示 recommendedResources 列表，每项有「生成」跳转链接 |
| 点击「生成」链接 | 跳转至资源生成页并预填主题 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-07 个人知识库上传与检索

| 步骤 | 预期结果 |
|------|---------|
| 进入「知识库」，上传 PDF 文件 | 上传成功，显示文件名和状态 |
| 在辅导页提问相关内容 | RAG 检索到知识库内容，引用原文 |
| 删除文件 | 文件从列表消失 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-08 答题与错题本

| 步骤 | 预期结果 |
|------|---------|
| 进入课程详情，点击「开始答题」 | 显示练习题列表 |
| 答错部分题目，提交 | 错题自动入错题本，quiz_attempts 写入 topic_name |
| 进入「评估」页 | 薄弱点标签云显示错误率 >= 50% 的知识点 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-09 学习效果评估（AI 综合评估）

| 步骤 | 预期结果 |
|------|---------|
| 进入「学习评估」，点击「运行 AI 评估」 | 调用 deepseek-evaluate，显示 Agent 执行进度 |
| 等待完成 | 雷达图更新，显示 5 维度评分 |
| 查看「薄弱点标签云」 | 显示基于答题记录的真实薄弱点 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-10 可视化图解生成（Mermaid）

| 步骤 | 预期结果 |
|------|---------|
| 进入「可视化生成」，输入「TCP 三次握手」 | AI 生成 mermaid 代码 |
| 查看预览区 | Mermaid 流程图正确渲染为 SVG |
| 点击「复制代码」 | 剪贴板包含 mermaid 代码 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-11 PPT 课件生成

| 步骤 | 预期结果 |
|------|---------|
| 进入「资源生成」，选「PPT 课件」，输入主题 | 调用 generate-ppt Edge Function |
| 等待完成 | 显示幻灯片大纲预览 |
| 点击「下载 PPT」 | 下载 .pptx 文件，可在 PowerPoint 打开 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-12 RLS 权限验证 + JWT 鉴权

| 步骤 | 预期结果 |
|------|---------|
| 用户 A 登录，创建笔记和学习记录 | 数据正常保存 |
| 用户 B 登录，尝试直接访问 A 的数据 | 返回空列表（RLS 阻断） |
| Edge Functions 不再信任请求体 userId | 必须提供有效 JWT token |
| 直接调用 Supabase REST API 不带 token | 401 Unauthorized |

**结果**：✅ 手动通过（2026-07-12 · v13 已升级 JWT 鉴权中间层）

---

### TC-13 防幻觉审核（ReviewerAgent）

| 步骤 | 预期结果 |
|------|---------|
| 生成资源后，查看 Agent 执行链 | ReviewerAgent 显示审核评分 |
| 审核 score >= 70 | verdict 为 pass，内容正常展示 |
| 审核 score < 70 | 前端显示 warn/reject 标识和改进建议 |

**结果**：✅ 手动通过（2026-07-12）

---

### TC-14 系统状态检查（真实健康检查）

| 步骤 | 预期结果 |
|------|---------|
| 进入「系统状态」页 | 显示真实健康检查结果（非硬编码全绿） |
| 查看核心服务状态 | DB/DeepSeek/Spark/RAG/Storage 各自显示真实状态 |
| 点击刷新 | 重新拉取最新状态 |

**结果**：✅ 手动通过（2026-07-12 · v13 已接入 system-health Edge Function）

---

### TC-15 错误监控（Sentry）

| 步骤 | 预期结果 |
|------|---------|
| 触发前端 JS 错误 | Sentry 控制台捕获错误 |
| 查看 Sentry 事件详情 | 包含 stack trace、用户 ID、页面 URL |

**结果**：✅ 手动通过（2026-07-12）

---

## 4. 测试结果汇总

| 用例编号 | 功能模块 | 手动验证 | 自动测试 |
|---------|---------|---------|---------|
| TC-01 | 登录/注册 | ✅ 通过 | ⚠️ 待自动化 |
| TC-02 | 学习画像构建 | ✅ 通过 | ⚠️ 待自动化 |
| TC-03 | SSE 流式辅导 + Mermaid | ✅ 通过 | ⚠️ 待自动化 |
| TC-04 | 多智能体 DAG 资源生成 | ✅ 通过 | ⚠️ 待自动化 |
| TC-05 | DAG 容错（单 Agent 失败） | ✅ 通过 | ⚠️ 待自动化 |
| TC-06 | 学习路径 + 推荐资源 | ✅ 通过 | ⚠️ 待自动化 |
| TC-07 | 个人知识库上传检索 | ✅ 通过 | ⚠️ 待自动化 |
| TC-08 | 答题 + 错题本 + 薄弱点 | ✅ 通过 | ⚠️ 待自动化 |
| TC-09 | AI 综合评估 | ✅ 通过 | ⚠️ 待自动化 |
| TC-10 | 可视化图解（Mermaid） | ✅ 通过 | ⚠️ 待自动化 |
| TC-11 | PPT 课件生成 | ✅ 通过 | ⚠️ 待自动化 |
| TC-12 | RLS + JWT 权限隔离 | ✅ 通过 | ⚠️ 待自动化 |
| TC-13 | 防幻觉审核 | ✅ 通过 | ⚠️ 待自动化 |
| TC-14 | 系统状态（真实健康检查） | ✅ 通过 | ⚠️ 待自动化 |
| TC-15 | Sentry 错误监控 | ✅ 通过 | ⚠️ 待自动化 |

**手动验证：15 / 15 通过**  
**自动化测试：0 / 15（建设中，预计使用 Vitest + Playwright + Deno test）**

---

## 5. 已知问题与后续优化

| 编号 | 描述 | 状态 | 优先级 |
|------|------|------|--------|
| KI-01 | PathPage 重规划提示（进度变化 >=20%）在首次加载时可能误触发 | 已知 | 低 |
| KI-02 | TutorPage Mermaid 渲染在极长图表时可能溢出卡片宽度 | 待修复 | 低 |
| KI-03 | 自动化测试套件待建设（Vitest + Playwright + Deno test） | 待建设 | P1 |
| KI-04 | RAG search-knowledge 生产环境不再静默 fallback demo 数据（v13 已修复） | ✅ 已修复 | — |
| KI-05 | Edge Functions 统一 JWT 鉴权中间层（v13 已修复） | ✅ 已修复 | — |
| KI-06 | StatusPage 已接入真实 system-health API（v13 已修复） | ✅ 已修复 | — |
| KI-07 | package.json scripts 已恢复 dev/build/typecheck/test/verify（v13 已修复） | ✅ 已修复 | — |
